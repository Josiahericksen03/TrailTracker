disable firewall: sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off
enable firewall: sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on

FLASK_APP=app.py flask run --host=0.0.0.0 --port=8080

In order:
GRAPH PROJECT:
Second phase:
-show all camera ids even if 0 when sorting by animal
-add/deletion of cameras should modify the graph by appearing/disappearing on it
-will suggest "based on entered "Deer" you should go to camera 3

SETTINGS:
-optional change pin colors, logos


OTHER TWEAKS:
-fix upload button, make green again, close gap size between upload and graph
-fix grey box size on edit pin modal in home



AI:
-train ai off of rest of footage
-also look at other ways to effectively train it

-figure out how to turn into progressive webapp
-get to iphone(convert to react native and django? or keep mongodb)
-done


